<!DOCTYPE html>
<html>
  <head>
    <title>Professional Coin Galleries</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta
      name="viewport"
      content="minimum-scale=1, initial-scale=1, width=device-width"
    />
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
